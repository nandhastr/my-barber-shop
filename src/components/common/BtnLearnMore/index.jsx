import React from 'react'

const BtnLearnMore = ({className, text}) => {
  return <div className={`${className} transition-all duration-300 ease-in-out`}>{text}</div>;
}

export default BtnLearnMore
